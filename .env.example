# Fichier d'exemple pour les variables d'environnement.
# Copiez ce fichier en .env et renseignez les valeurs si nécessaire.
# Ne jamais committer le fichier .env (il est dans .gitignore).

# Exemple (non utilisé pour l'instant — aucune clé API payante requise en Phase 0-3)
# YFINANCE_CACHE_DIR=./data/cache
